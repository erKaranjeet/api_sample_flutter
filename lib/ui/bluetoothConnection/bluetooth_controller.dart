import 'package:flutter_blue_plus/flutter_blue_plus.dart';
import 'package:get/get.dart';

class BluetoothController extends GetxController {

  // FlutterBluePlus bluePlus = FlutterBluePlus.instance;

  Future scanDevices() async {
    // bluePlus.startScan(timeout: const Duration(seconds: 20));
    // bluePlus.stopScan();
  }

  // Stream<List<ScanResult>> get scanResults => bluePlus.scanResults;
}